{
  "extends": "../../tsconfig.base.json",
  "comment": "Emit is handled by esbuild (see build script). tsc is used only for type-checking (lint). Bundler resolution lets the @trpc/server/adapters/* subpaths resolve via package exports maps.",
  "compilerOptions": {
    "lib": ["ES2022"],
    "types": ["node"],
    "noEmit": true,
    "sourceMap": false
  },
  "include": ["src/**/*.ts"],
  "exclude": ["dist", "node_modules"]
}
