{
  "name": "@openmasjid/core",
  "version": "0.2.0",
  "private": true,
  "description": "OpenMasjidOS core daemon — Fastify + tRPC control plane that manages Docker apps and serves the dashboard.",
  "type": "commonjs",
  "main": "dist/index.js",
  "types": "src/trpc/router.ts",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "esbuild src/index.ts src/reset-password.ts --bundle --platform=node --target=node20 --format=cjs --packages=external --outdir=dist --log-level=warning",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "echo \"(no core tests yet)\""
  },
  "dependencies": {
    "@fastify/cookie": "^11.0.1",
    "@fastify/multipart": "^9.0.1",
    "@fastify/static": "^8.0.3",
    "@fastify/websocket": "^11.0.1",
    "@node-rs/argon2": "^2.0.2",
    "@trpc/server": "^11.0.0",
    "dockerode": "^4.0.2",
    "fastify": "^5.1.0",
    "fflate": "^0.8.2",
    "systeminformation": "^5.23.5",
    "ws": "^8.18.0",
    "yaml": "^2.6.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/dockerode": "^3.3.31",
    "@types/node": "^20.16.5",
    "@types/ws": "^8.5.13",
    "esbuild": "^0.24.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
